services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: dtu_grades
      POSTGRES_USER: dtu
      POSTGRES_PASSWORD: dtu_dev
    ports:
      - "5433:5432"
    volumes:
      - postgres_dev_data:/var/lib/postgresql/data

volumes:
  postgres_dev_data:
